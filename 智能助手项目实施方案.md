# 智能助手项目 — 实施方案（评审版）

> 版本：v1.0 ｜ 场景：多智能助手统一 + 知识管理能力增强 ｜ 平台：Dify 1.16 ｜ 人力：2 人（AI 辅助）
> 本文档整合：需求拆分 / 工作量评估 / 甘特排期 / 开发期任务清单 / 技术架构。

---

## 1. 项目概述与结论

围绕 6 项需求，结论如下：

| 序号 | 需求 | 结论 | 实施要点 |
|---|---|---|---|
| 1 | 多智能助手合并 | 可以，需定制 | 资料类 → 统一入口 + 多库融合检索；技术体系 → 知识图谱独立建设（保护准确率） |
| 2 | 参考文献查找与下载 | 可以，需定制 | 问答标注来源 + 原文档下载（带权限校验） |
| 3 | 知识查询权限设置 | 可以，需定制 | 四层授权：部门—角色—知识域/文档—操作；粒度需业务方确认 |
| 4 | 知识库使用统计 | 可以，需定制 | 接口调用量、知识库调用情况埋点统计 |
| 5 | 日志（Log）功能 | 可以，需定制 | 问答/检索/下载/权限变更/系统 5 类审计日志可查 |
| 6 | PPT/Word 智能生成 | 建议方案一 | 采购 WPS/豆包类 + 集成（见效快）；不建议开源自研 6 人月 |

**关键决策**：平台沿用 Dify，不换 harness / 不自研底座。第 6 项走采购，省下精力投到权限与检索（项目护城河）。

---

## 2. 现状勘察（基于实际代码，重要前提）

### 2.1 可复用的 Dify 既有能力
| 能力 | 位置 |
|---|---|
| RBAC 装饰器 `rbac_permission_required` | `api/controllers/common/wraps.py` |
| 企业版 RBAC 服务 | `api/services/enterprise/rbac_service.py` |
| 数据集权限表 `DatasetPermission`（`dataset_permissions`） | `api/models/dataset.py` L1439 |
| 权限枚举 `PermissionEnum`（`ONLY_ME/ALL_TEAM/PARTIAL_TEAM`） | `api/models/enums.py` L386 |
| 现有权限校验 `DatasetService.check_dataset_permission` | `api/controllers/console/datasets/datasets.py` |
| 检索服务 `RetrievalService`（`retrieve/_retrieve/external_retrieve`） | `api/core/rag/datasource/retrieval_service.py` |
| 埋点通道 `core/ops/`、结构化日志 `core/logging/` | `api/core/` |

### 2.2 关键缺口（需新建）
| 缺口 | 说明 |
|---|---|
| **组织/部门概念不存在** | `Account` 无 department；`core/rbac/entities.py` 仅 81 行 |
| **权限粒度止于 account 级** | `dataset_permissions` 只到 account_id，无"部门—角色—域/文档—操作"四层 |
| **检索层无权限过滤** | `retrieval_service.py` 无 permission 逻辑 → 需补（安全底线） |

---

## 3. 技术架构

```
┌──────────────────────────── 前端 (web/app) ──────────────────────────────┐
│  统一助手入口页    知识权限配置页    统计仪表盘    日志查询页              │
└──────────────────────────────────────────────────────────────────────────┘
                              │ REST
┌──────────────────────────── API 层 ──────────────────────────────────────┐
│  console controllers: datasets/ rag_pipeline/ departments/                │
│  service_api: 检索下载端点                                                    │
│  ★ kb_permission_required 装饰器（检索/下载/管理接口）                      │
└───────────────┬──────────────────────────────────────────────────────────┘
┌───────────────▼──────────────── 核心服务层 ──────────────────────────────┐
│  RetrievalService.retrieve（已有）                                        │
│   ├─★ 检索前 subject 过滤（权限白名单注入）★安全核心                        │
│   └─ FusionService 多库融合检索（归一化+去重+排序）                        │
│  KB_PermissionService（四层授权解析） / GraphService（技术体系图谱检索）    │
└───────────────┬──────────────────────────────────────────────────────────┘
┌───────────────▼──────────────── 数据层 ──────────────────────────────────┐
│  department / department_member（新表） / kb_permission_grants（新表）    │
│  现有 dataset/document/segment + dataset_permissions（不动）               │
│  向量库(现有) 图存储(新增可选) 埋点统计表、审计日志表（新增）              │
└───────────────────────────────────────────────────────────────────────────┘
```

**架构决策**：
1. 新增独立四层授权表，**不动现有 `dataset_permissions`**（保留账号级兼容）。
2. 新建独立 `@kb_permission_required`，不污染现有 RBAC 语义。
3. **检索层隔离必做**，不能只拦 controller（防服务直连绕过）。
4. 资料融合检索与图谱检索并行但独立，不混合排序。

---

## 4. 需求拆分与工作量

### 4.1 公共前置（P0）
| 项 | 工作量（AI 辅助/人） |
|---|---|
| 组织模型（部门/成员/树） | ~1 周 |
| 前端统一入口聚合层 | ~1 周 |

### 4.2 各项工作量（AI 辅助、单人周）
| 项 | 范围 | AI 后耗时 |
|---|---|---|
| 需求 1 | 多库融合检索 + 知识图谱 + 评测基线 | ~4 周 |
| 需求 2 | 来源标注 + 原文档下载 | ~2 周 |
| 需求 3 | 四层授权模型 + 装饰器 + 检索隔离 + 权限页 | ~4 周 |
| 需求 4 | 计量埋点 + 统计接口 + 仪表盘 | ~1.5 周 |
| 需求 5 | 5 类审计日志 + 查询页 | ~1.5 周 |
| 需求 6 | 外部生成服务集成（采购） | ~1 周 |

> 2 人并行 + AI 辅助，**估算开发周期约 12~14 个日历周（≈3 个月）**。
> 说明：AI 加速码量，但压缩不掉串行依赖、安全审查、知识图谱调优、联调验收。

---

## 5. 甘特排期（2 人，A/B 两线并行）

```
月       1        2        3
人1(权限): P0 ─▶ 3.1模型→3.2接入→3.4检索隔离 ─▶ 2.下载/5日志/权限页
人2(检索): P0统一入口 ─▶ 1.1融合检索→1.2图谱 → 2来源→4统计→6生成
里程碑:   M1 底座+授权模型+融合检索可用
          M2 权限接通全部目标接口
          M3 全部 6 项上线+联调验收
```

**依赖链**：P0 → 3.1 → 3.2 → 3.4（强串行）；P0 → 1.1 → 2 ~ 4；3.x → 5.x。

---

## 6. 开发期任务清单（开发认领用）

### 阶段 A：P0 底座
| # | 任务 | 落点 |
|---|---|---|
| A1 | 部门/成员模型 + 部门树 CRUD API | `api/models/department.py`、`api/controllers/console/departments.py` |
| A2 | [前端] 统一助手入口 + 路由 | `web/app/(commonLayout)/datasets/` |
| A3 | 迁移 + 初始化部门数据 | `api/migrations/` |

### 阶段 B：需求 3 授权（★★★ 核心）
| # | 任务 | 落点 |
|---|---|---|
| B1 | 四层授权模型 `kb_permission_grants` + 父部门继承 | `api/models/kb_permission.py` |
| B2 | 权限解析 `KB_PermissionService`（默认 deny，最高优先显式授权） | `api/services/kb_permission_service.py` |
| B3 | ★ 装饰器 `@kb_permission_required` | `api/controllers/common/wraps.py` 旁 |
| B4 | ★ 挂装饰器到检索/下载/管理接口 | `datasets.py`、`datasets_document.py`、`rag_pipeline.py` |
| B5 | ★★ 检索层隔离（白名单注入，安全底线） | `api/core/rag/datasource/retrieval_service.py` |
| B6 | [前端] 知识权限配置页（部门树×域/文档授权） | `web/app` |
| B7 | 权限粒度 MVP 与业务方对齐评审 | — |

### 阶段 C：需求 1 检索/图谱
| # | 任务 | 落点 |
|---|---|---|
| C1 | 评测基线（query 集 + 人工打分） | `dev/` 评测脚本 |
| C2 | ★ 多库融合检索 `FusionService` | `api/core/rag/retrieval/fusion_service.py` |
| C3 | [前端] 接入融合检索入口 | `web/app` |
| C4 | 知识图谱子模块（实体抽取 + 图检索） | `api/core/rag/graph/` |
| C5 | 图谱权限隔离（复用 B5 白名单） | `api/core/rag/graph/` |

### 阶段 D：需求 2/4/5/6（B5 后并行）
| # | 任务 | 落点 |
|---|---|---|
| D1 | 来源标注（引用元数据 + 前端问答卡片） | `RetrievalService` 返回结构扩展 |
| D2 | ★ 原文档下载（权限校验 + 二进制流转发） | `controllers/console/datasets/`、`extensions/ext_storage` |
| D3 | 计量埋点（复用 `core/ops/`） | `core/ops/`、`retrieval_service.py` |
| D4 | 统计聚合接口 + [前端]仪表盘 | `models/`、`controllers`、`web/app` |
| D5 | 5 类审计日志（复用 `core/logging/`） | `models/`、`core/logging/` |
| D6 | [前端] 日志查询页（筛选/导出） | `web/app` |
| D7 | PPT/Word 生成集成（采购 SDK） | `api/core/` + endpoint、`web/app` |

---

## 7. 开发顺序（2 人）

```
人1(权限核心): B1→B2→B3→B4→B5 【串行，最重要】 ─完成后→ D2/D5/B6
人2(检索/前端): C1→C2→C3→C4→C5 ─B5后→ D1/D3/D4/D6/D7
最终 D 阶段与 B5 联调验收
```

---

## 8. 关键风险与决策点

| 风险/决策 | 影响 | 建议 |
|---|---|---|
| 权限粒度未定（B7） | 决定 B 阶段工作量上限 | 先出 MVP（部门/账号 × 知识域 × 检索/下载）再扩展 |
| B5 检索隔离缺失 | 安全洞，服务直连绕过权限 | 必须在 C2 联调前完成 |
| 融合准确率不可量化（C1） | 验收说不清 | 必须建评测基线 |
| 知识图谱成本（C4） | 图数据库运维负担 | 规模小先用关系表模拟，再切图数据库 |
| 第 6 项方案 | 品控 vs 周期 | 走采购方案一 |

---

## 9. 交付物清单
- 后端：组织模型、四层授权 API、权限装饰器、检索层隔离、融合检索、图谱检索、下载、埋点统计、审计日志、生成服务集成
- 前端：统一入口、权限配置页、统计仪表盘、日志查询页、生成入口
- 数据：迁移文件、权威测评基线、单元测试（权限/安全回归）

---

# 10. 实施进展追踪（工作区实时）

> 记录 `feature/kb-permission` 分支最新落地情况，随开发持续更新。最后更新：**前端首个切片完成（B6 权限配置页）**——后端已收口，切入前端同步阶段，已完成 B6。

## 10.1 完成度速览

| 阶段 | 任务 | 状态 | 说明 |
|---|---|---|---|
| A（P0） | A1 部门/成员模型 + CRUD | ✅ | `api/models/department.py` + `departments.py` + `department_service.py` |
|  | A2 [前端]统一入口 | ✅ | `/datasets/unified` 统一融合检索入口（kb-fusion，见 10.7） |
|  | A3 迁移/初始化 | ✅ | 建表迁移✅；`create_tenant` 自动建根部门"全公司"种子✅ |
| B（需求3） | B1 授权模型 | ✅ | `kb_permission_grants`（默认 deny），未动 `dataset_permissions` |
|  | B2 权限服务 | ✅ | `KBPermissionService`，部门祖先链继承 |
|  | B3 ★装饰器 | ✅ | `api/controllers/common/kb_wraps.py`，独立于企业 RBAC |
|  | B4 装饰器挂载 | ✅ | datasets / datasets_document / kb_graphs / 下载 |
|  | B5 ★检索隔离 | ✅ | `retrieval_service` + `hit_testing` + `FusionService` 白名单 |
|  | B6 [前端]权限配置页 | ✅ | 完成首个前端切片：部门树 + 授权管理页（见 10.5） |
|  | B7 MVP 业务评审 | 🔶 | 待业务方确认 |
| C（需求1） | C1 评测基线 | ❌ | `dev/` 无脚本 |
|  | C2 ★融合检索 | ✅ | `FusionService`（RRF/Max/Sum） |
|  | C3 [前端]融合入口 | ✅ | `kb-fusion` 页面 + `/workspaces/current/kb-fusion/retrieve`（见 10.7） |
|  | C4 图谱子模块 | ✅ | `api/core/rag/graph/`，关系表实现 |
|  | C5 图谱权限隔离 | ✅ | 复用 B5 白名单 |
| D（2/4/5/6） | D1 来源标注 | ✅ | 后端✅；前端问答引用卡片由 kb-fusion 结果卡片（标题/得分/来源/内容）承载（见 10.7） |
|  | D2 ★原文档下载 | ✅ | 含流式 `/download-file` 端点 |
|  | D3 计量埋点 | ✅ | 复用 `dataset_queries` + `audit_logs` 统计 |
|  | D4 统计接口+仪表盘 | ✅ | 仪表盘完成（见 10.5）：KB 调用 Top / 趋势 / 接口事件三卡片 |
|  | D5 审计日志 | ✅ | 表/服务/查询API + **运行时接线已补** |
|  | D6 [前端]日志查询页 | ✅ | 审计日志查询页完成（筛选/分页，见 10.5） |
|  | D7 PPT/Word 生成 | ❌ | 采购方案，未开始 |

## 10.2 后端已落地要点

- 四层授权（subject: 账号/部门/角色 × resource: 数据集/文档 × action），**默认拒绝**，部门授权沿祖先链下传。
- 检索层隔离为安全底线：`retrieval_service.retrieve` 传入交互账号即强制白名单，`FusionService` 也做白名单交集——服务直连无法绕过。
- 3 个迁移：部门/角色/授权表、审计日志表、图谱表。
- 单元测试：权限/审计/统计/融合/图谱/检索等**相关模块 191 passed**（含新增 QNA 审计接线与部门种子用例）。

## 10.3 审计日志运行时接线（D5 补全，最近完成）

此前审计仅有表/服务/查询API（只读，未落库）；现已接入 5 类事件：

| 审计类 | 动作 | 位置 |
|---|---|---|
| QNA | 终端用户经 App/工作流触发问答检索 | `core/rag/retrieval/dataset_retrieval.py`（按 app + 命中数据集落一条） |
| DOWNLOAD | 签名URL / 流式下载 / ZIP下载 | `controllers/console/datasets/datasets_document.py` |
| PERMISSION_CHANGE | 授权 / 撤销 | `controllers/console/kb_access_policies.py` |
| PERMISSION_CHANGE | 部门增删改 / 加成员 | `controllers/console/departments.py` |
| RETRIEVAL | 命中测试 / 融合检索 / 图谱检索 | `hit_testing_service.py` / `fusion_service.py` / `kb_graphs.py` |
| SYSTEM | 图谱构建 / 清空 | `controllers/console/kb_graphs.py` |

每条审计自动从 `core.logging` 补齐 `request_id / trace_id / ip` 与结构化日志关联。

## 10.4 剩余待办（按优先级）

前端切片已全部完成：**B6 权限配置页、D6 审计日志查询页、D4 统计仪表盘、A2/C3 统一融合检索入口、D1 问答引用卡片**（见 10.5/10.6/10.7）。剩余：
1. **C1 评测基线**未建立 —— 与方案风险项冲突，必须建。
2. **B7 权限粒度 MVP** 待业务方确认。
3. **D7** 采购集成未开始。
4. 当前改动均在 `feature/kb-permission` 工作树，**尚未提交**，建议尽快提交并附本方案对照说明。

## 10.5 前端进展（B6 权限配置页，最近完成）

首个前端垂直切片完成 —— **「知识库访问设置 / Knowledge Access Settings」页面**，打通四层授权管理 UI。后端自定义端点（`kb-permission-grants`、`departments`）不在生成的 ORPC 合约内，故走 `web/service/base` 的 `get/post/patch/del` 原生请求层。

### 10.5.1 界面与功能
- **路由页**：`web/app/(commonLayout)/datasets/permissions/page.tsx`（置于 datasets 公共布局下，不冲突 `(datasetDetailLayout)` 路由组）。
- **入口**：datasets 列表页 header 新增「知识库访问设置」链接按钮 → `/datasets/permissions`。
- **部门 Tab**：部门树增删改 + 展开折叠 + 成员管理（`department-panel.tsx`）。
- **访问策略 Tab**：主体（成员/部门）× 资源（数据集）× 操作（多选）授权 + 现有授权列表逐条撤销（`grant-panel.tsx`）。

### 10.5.2 代码结构（`web/app/components/datasets/kb-permission/`）
| 文件 | 说明 |
|---|---|
| `types.ts` | 部门/授权 DTO 类型 |
| `services.ts` | 原生 `request` 层封装（部门 CRUD、成员、授权/撤销） |
| `department-panel.tsx` | 部门树 + 成员管理 |
| `grant-panel.tsx` | 授权/撤销管理 |
| `index.tsx` | 页壳 + Tab 切换 |
| `__tests__/` | 2 个 RTL 测试（grant-panel、department-panel） |

### 10.5.3 i18n
- 新增 `datasetPermission` 命名空间，覆盖全部 24 个 locale（`en-US` 权威、`zh-Hans` 完整翻译、其余以 en-US 结构性填充 + `fallbackLng: en-US` 兜底）。
- 已在 `web/i18n-config/resources.ts` 注册（import + `RawResources` + `namespaces`），并同步 header 入口按钮文案。

### 10.5.4 验证与说明
- 24 个 locale JSON 均有效、key 一致性通过；组件内引用全部 i18n key 均存在；import 路径有效；新 TSX 括号/结构平衡。
- **⚠️ 前端测试未在当前环境执行**：工作区未安装 `node_modules`（需联网安装 web 依赖，环境受限）。测试已按仓库约定（全局 `react-i18next` mock 返回 `ns.key`、直接 mock `@tanstack/react-query` 与 dify-ui 原语）编写，需在具备依赖机器 `cd web && pnpm install && pnpm test` 确认。

### 10.5.5 待补/后续切片
- 授权主体当前开放**成员 / 部门**（二者有现成数据源）；`role` 主体后端已支持但前端暂无角色列表端点，待接入后再开放。
- 后续切片（统一入口/融合入口/引用卡片）已由 `/datasets/unified`（kb-fusion）完成，见 10.7。

## 10.6 前端进展续（D6 审计日志查询页 + D4 统计仪表盘，最近完成）

沿用 B6 的 route + service + i18n 切片模式，再完成两个 owner/admin 页面。

### 10.6.1 D6 审计日志查询页
- **路由**：`web/app/(commonLayout)/datasets/audit-logs/page.tsx`；组件 `web/app/components/audit-logs/`。
- **功能**：类型/状态/操作/资源 ID 筛选 + 分页表格（时间/类型/操作/状态/用户/资源/IP）。
- **API**：`GET /workspaces/current/audit-logs`（`log_type`/`action`/`status`/`resource_id`/`page`/`page_size`）。
- **i18n**：新增 `auditLogs` 命名空间（24 locale 全量 + `resources.ts` 注册）。
- **测试**：`__tests__/index.spec.tsx`（表格渲染/空态/筛选/分页）。

### 10.6.2 D4 统计仪表盘
- **路由**：`web/app/(commonLayout)/datasets/statistics/page.tsx`；组件 `web/app/components/usage-statistics/`。
- **功能**：7/30/90 天切换 + 三卡片（KB 调用 Top-N、按天趋势柱状图、接口/事件调用按类型）。
- **API**：`/statistics/knowledge-base/calls`、`/statistics/knowledge-base/trend`、`/statistics/interfaces/calls`。
- **i18n**：新增 `usageStatistics` 命名空间（24 locale 全量 + `resources.ts` 注册）。
- **测试**：`__tests__/index.spec.tsx`（卡片渲染/数据行/时间范围切换）。

### 10.6.3 入口
- datasets 列表 header 新增「审计日志」「使用统计」入口按钮 → `/datasets/audit-logs`、`/datasets/statistics`，header.spec 新增路由断言。

### 10.6.4 验证与说明
- 两个命名空间 24 locale 均有效、key 一致性通过，组件引用 key 全部存在；新 TSX 括号/结构平衡。
- **⚠️ 前端测试未在当前环境执行**（未装 `node_modules`），需具备依赖机器 `cd web && pnpm install && pnpm test` 确认。

## 10.7 前端进展（A2 统一入口 + C3 融合检索 + D1 引用卡片，`/datasets/unified`）

统一助手入口与融合检索已落地为 `/datasets/unified`（组件 `web/app/components/datasets/kb-fusion/`）。

### 10.7.1 实现
- **路由**：`web/app/(commonLayout)/datasets/unified/page.tsx` → `KBFusionSearch`。
- **功能**：一个提问框跨全部「已授权」知识库执行融合检索（默认 RRF，可切 Max/Sum），结果去重、带来源卡片（标题/得分/内容/所属数据集=引用卡片）。含 AbortController 取消、加载/空态/错误重试。
- **API**：`POST /workspaces/current/kb-fusion/retrieve`（`kb_fusion.py`，检索层 B5 白名单过滤，owner/admin 不限）。
- **i18n**：`datasetFusion` 命名空间，24 locale 全量 + `resources.ts` 注册。
- **入口**：datasets header「统一助手检索」按钮 → `/datasets/unified`（与权限/统计/日志并列）。
- **测试**：`__tests__/index.spec.tsx`。

### 10.7.2 切片对齐
- **A2 统一助手入口 + 路由** ✅：`/datasets/unified` 作为多智能助手统一入口页。
- **C3 融合检索入口** ✅：调用 `FusionService` 的统一检索 UI。
- **D1 来源标注 / 问答引用卡片** ✅：结果卡片展示每条命中的来源（标题/数据集/得分/内容片段）即“引用卡片”；后端 `FusionRecord.metadata`/`SourceMetadata` 提供来源元数据。

### 10.7.3 验证
- `datasetFusion` 24 locale 均有效、key 一致性通过；组件引用的 key（含 `strategyRRF/Max/Sum`、`resultCount` 插值）全部存在。
- **⚠️ 前端测试未在当前环境执行**（未装 `node_modules`），需具备依赖机器 `cd web && pnpm install && pnpm test` 确认（含 kb-fusion、header 路由断言）。

## 10.8 数据库访问 / demo 数据 / 表命名

### 10.8.1 数据库访问（DBeaver 直连）
给 `db_postgres` 服务加了宿主机端口映射（`docker/docker-compose.yaml` 中 `EXPOSE_DB_PORT`，默认 `5432`）：

| 项 | 值 |
|---|---|
| 主机 / 端口 | `localhost:5432` |
| 数据库 | `dify` |
| 用户 / 密码 | `postgres` / `difyai123456` |
| 连接串 | `postgresql://postgres:difyai123456@localhost:5432/dify` |

> 用于日常所有 Dify 业务数据 + 本项目新增 `kb_*` 表。含用户/租户/知识库数据：`lirui's Workspace`、`harrydolly1226@gmail.com`、知识库 `test`、文档 `可视化算子分类建议.md`。

### 10.8.2 新表命名（kb_ 前缀）
定制新增表统一带 `kb_` 前缀，避免与上游/企业版 RBAC（`roles`、`operation_logs` 等）撞名，降低未来 merge 冲突：

| 表 | 说明 |
|---|---|
| `departments` / `department_members` | 组织树 + 成员 |
| `kb_roles` / `kb_user_roles` | 自定义角色 + 账号绑定 |
| `kb_permission_grants` | 四层授权（subject × resource × action，默认 deny） |
| `kb_audit_logs` | 5 类审计日志 |
| `graph_entities` / `graph_relations` | 知识图谱（关系表） |

> 说明：`roles→kb_roles`、`user_roles→kb_user_roles`、`audit_logs→kb_audit_logs` 三张已按此重命名（模型 `__tablename__`、迁移 DDL、demo 脚本、数据库表与索引均已同步）。**模型类名保留**（`Role/UserRole/AuditLog`），Python 引用零改动。

### 10.8.3 Demo 数据（`demo_kb_data.sql`，幂等可重跑）
已灌入关联现有租户/账号/知识库 `test` 的 demo 数据：

| 表 | 数量 | 内容 |
|---|---|---|
| departments | 4 | 全公司 → 研发部/数据部；研发部 → 可视化算法组 |
| department_members | 1 | 账号 ∈ 研发部 |
| kb_roles | 2 | 知识管理员 / 普通用户 |
| kb_user_roles | 1 | 账号 → 知识管理员 |
| kb_permission_grants | 11 | account/department/role × dataset/document × 各类 action |
| kb_audit_logs | 5 | qna / retrieval / download / permission_change / system 各一 |
| graph_entities | 5 | 可视化算子、通用算子、业务算子、分类范围、系统算子 |
| graph_relations | 4 | 属于 / 区别于 / 界定 / 排除于 |

> 相关既有单元测试全部通过（28 passed 覆盖权限/审计/融合/检索 on_query），模型与迁移编译通过，旧表名无残留引用。
